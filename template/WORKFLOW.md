# WORKFLOW.md — Claude Code Quick Reference

This file is a quick reference for the project owner.
Full instructions for CC — in `CLAUDE.md`. Skill details — in `.claude/skills/meta/`.

---

## Initial setup

After deploying the template, fill in the placeholders below before the first session.
Files generated by CC commands (`status.md`, `plan.md`) are not listed.

| Placeholder | File | Purpose | Example |
| --- | --- | --- | --- |
| `{PROJECT_NAME}` | `CLAUDE.md`, `.claude/index.md`, `.context/*.md` | Project name — appears in headings across all files | `payment-service` |
| `{COMMUNICATION_LANGUAGE}` | `CLAUDE.md` | Language CC uses in chat | `English` |
| `{CONTEXT_LANGUAGE}` | `CLAUDE.md` | Language for `.context/` files | `Russian` |
| `{CODE_COMMENTS_LANGUAGE}` | `CLAUDE.md` | Language for code comments | `English` |
| `{PROJECT_DESCRIPTION}` | `CLAUDE.md` | 1–2 sentences: what the product is, who the user is, core mechanic | `REST API for task tracking. Users create and assign tasks via Telegram bot.` |
| `{REPO_URL}` | `CLAUDE.md` | Repository URL | `https://github.com/org/payment-service` |
| `{LAYER_*}` / `{TECH_*}` | `CLAUDE.md` | Stack table rows — add or remove rows as needed | `Backend` / `FastAPI`, `DB` / `PostgreSQL` |
| `{ARCHITECTURE_RULES}` | `CLAUDE.md` | Key constraints CC must follow: data flow, layer boundaries, state management | `Business logic not in routers. State in Redis.` |
| `{STATE_STORAGE}` | `CLAUDE.md` | Where application state lives (used inside architecture rules) | `Redis` |
| `{RUNTIME_SETUP}` | `CLAUDE.md` | Table of dev commands: install, run, test | `uv sync` / `uv run pytest` |
| `{CODE_CONVENTIONS}` | `CLAUDE.md` | Style and naming rules, commit format | `Type hints everywhere. Commits: type: description.` |
| `{PROJECT_STRUCTURE}` | `CLAUDE.md` | Directory tree of the project | `src/ tests/ .claude/ .context/` |
| `{PROJECT_DESCRIPTION_FULL}` | `.context/blueprint.md` | Extended description: system purpose, users, key principles | See inline guidance in file |
| `{PRINCIPLE_*}` | `.context/blueprint.md` | Core design principles (2+ items) | See inline guidance in file |
| `{FIRST_KEY_DECISION}` | `.context/blueprint.md` | Summary of ADR-001 in the decisions table | See inline guidance in file |
| `{COMPONENT_*}` | `.context/blueprint.md` | Component names and section headings (3.1, 3.2, …) | See inline guidance in file |
| `{MAIN_FLOW}` | `.context/blueprint.md` | Name of the primary data flow (section 4.1) | See inline guidance in file |
| `{FIRST_DECISION_TITLE}` | `.context/decisions.md` | Title of ADR-001 | `Use PostgreSQL as primary database` |
| `{MVP_GOAL}` | `.context/to-do.md` | One-line MVP goal | `Working Telegram bot that creates and assigns tasks` |
| `{PHASE_*}` / `{PHASE_NAME}` | `.context/to-do.md` | Phase numbers and names in the task queue | `Phase 1` / `Core API` |
| `{TASK_*}` | `.context/to-do.md` | Task items within each phase | `Implement POST /tasks endpoint` |
| `{DEBT_ITEM_*}` | `.context/to-do.md` | Post-MVP technical debt items | `Add request rate limiting` |

Also worth setting up early:

- `.claude/skills/project/rules/` — drop `.md` files here for project-specific code rules
  applied by `/polish`. See the directory's `README.md` for the prose format and
  `no-dead-code.md` for a starter example. Remove the example when you have your own rules.

---

## Slash commands

Type `/command` directly in the Claude Code prompt. Each command is defined by a file in `.claude/commands/`.

| Command | What happens |
| --- | --- |
| `/organize` | Switch to Organizer mode — discuss workflows, edit `.claude/index.md` and skills |
| `/architect` | Switch to Architect mode — discuss architecture → write `.context/plan.md` |
| `/next` | Architect mode: suggest first incomplete item from `.context/to-do.md` |
| `/dev` | Switch to Developer mode — read `.context/plan.md`, create branch, write code |
| `/polish` | Clean up generated code — built-in `simplify` + project rules from `.claude/skills/project/rules/` |
| `/record` | Add ADR to `.context/decisions.md` |
| `/commit` | Show diff → wait for confirmation → commit |
| `/close` | Rebase + ff-merge into `dev`, delete branch |
| `/report` | Archive old `status.md`, write new one |
| `/sync` | Compare code with documentation, suggest changes |
| `/retro` | Analyze project history → write discussion → propose to-do and ADR updates |

---

## Branches

```text
main          ← releases (dev → main manually)
  └── dev     ← integration
        └── feature/<name>   ← all work goes here
        └── hotfix/<name>    ← urgent fixes → main
```

CC **never does `git push`** without explicit request.

---

## Typical workday

```text
0. Session start — CC reads automatically:
   .context/blueprint.md → .context/to-do.md → .context/status.md

1. Pick a task
   → /next
   CC reads to-do.md, suggests first incomplete item

2. Discuss
   → /architect
   CC asks questions, records decisions, writes .context/plan.md
   To record an architecture decision: /record

3. Implement
   → /dev
   CC creates branch feature/..., reads plan.md, writes code + tests

4. During work — commits
   → /commit
   CC shows what will be included, waits for confirmation, commits

4a. Before final commit — polish (optional)
   → /polish
   CC runs built-in simplify + project rules on the diff
   Presents findings per item: apply / skip / discuss
   Args: /polish <path> | /polish --staged | /polish (whole feature diff)

5. Task done
   → /close
   CC does rebase + ff-merge into dev, deletes branch

6. Every few tasks — sync documentation
   → /report  (update status.md)
   → /sync    (check blueprint / code divergence)

7. Periodically — retrospective
   → /retro
   CC reads decisions, history, git log — presents analysis draft
   You correct → CC writes .context/discussions/retro-YYYY-MM-DD.md
   Proposes to-do and ADR updates with per-item confirmation
```

---

## Commits

Format: `type: description`

| Type | When |
| --- | --- |
| `feat:` | new functional code |
| `fix:` | bug fix |
| `docs:` | documentation only |
| `refactor:` | refactoring without behavior change |
| `test:` | tests |
| `chore:` | infrastructure, configuration, dependencies |

---

## Project documentation

| File | Purpose |
| --- | --- |
| `CLAUDE.md` | Instructions for CC (read by CC, do not edit manually) |
| `.context/blueprint.md` | Project architecture |
| `.context/to-do.md` | Task queue |
| `.context/plan.md` | Current task |
| `.context/status.md` | Current implementation state |
| `.context/decisions.md` | Architecture decision history |
| `.context/notes/` | Owner's personal notes — not committed, CC does not read |
| `.claude/skills/project/rules/` | Project-specific code rules applied by `/polish` |
