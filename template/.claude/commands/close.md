Read `.claude/skills/meta/cc-close-task.md` and execute the algorithm.

Note: in this project `/close` merges `experiment/*` branches into `main` only.
Regular analysis work is committed directly to `main` — `/close` is not needed for it.
