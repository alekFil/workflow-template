## Task: Add placeholder reference to WORKFLOW.md

### Context

`template/WORKFLOW.md` is the quick reference for a new project owner.
Currently it documents commands, branches, and the daily workflow — but gives no
overview of what needs to be filled in before the first session.
A developer who just ran `install.sh` has no single place to see the full scope
of initial setup work.

Depends on: —

### What to implement

1. Add section `## Initial setup` to `template/WORKFLOW.md` before `## Slash commands`
2. Section contains: one-sentence intro + table of all setup placeholders
   (columns: Placeholder | File | Purpose | Example)

### Files

Edit:

- `template/WORKFLOW.md` — insert new section before `## Slash commands`

### Constraints

- Do not touch any other files
- `status.md` and `plan.md` are excluded — they are generated by CC commands
- Indexed placeholders (`{LAYER_*}`, `{TECH_*}`, `{PHASE_*}`, `{TASK_*}`, `{DEBT_ITEM_*}`,
  `{PRINCIPLE_*}`, `{COMPONENT_*}`) — one row each, not split by index
- `blueprint.md` placeholders: list name + purpose, example column says
  "see inline guidance in file" (no duplication of guidance text)
- Follow markdown conventions: `-` for lists, empty line before/after headings,
  language tag on all code blocks

### Verification

```bash
grep "## Initial setup" template/WORKFLOW.md
# expected: one match

grep "PROJECT_NAME\|COMMUNICATION_LANGUAGE\|ARCHITECTURE_RULES\|MVP_GOAL" template/WORKFLOW.md
# expected: all four found
```

### Changes along the way

(none)
