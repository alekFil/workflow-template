# Патч: упрощение команд CC и очистка README

Два независимых изменения из коммита `fe96487`.

---

## 1. CLAUDE.md — упростить активацию режима Архитектора

### Строка активации режима (раздел «Режим Архитектора»)

Было:

```text
Активируется фразой **"обсудим задачу"**, **"что дальше"** или **"следующая задача"**.
```

Стало:

```text
Активируется фразой **"обсудим"** (с любым продолжением или без).
```

### Таблица ключевых фраз

Было:

```text
| `обсудим задачу` | Переходи в режим Архитектора |
| `что дальше` | Переходи в режим Архитектора, предложи следующий шаг из `docs/to-do.md` |
| `следующая задача` | Переходи в режим Архитектора, возьми первый незавершённый пункт из `docs/to-do.md` |
```

Стало:

```text
| `обсудим` | Переходи в режим Архитектора |
| `что дальше` | Переходи в режим Архитектора, предложи первый незавершённый пункт из `docs/to-do.md` |
```

---

## 2. WORKFLOW.md — синхронизировать с CLAUDE.md

### Таблица режимов

Было:

```text
| `обсудим задачу`         | Архитектор  | Обсуждение архитектуры → план в `docs/plan.md`         |
| `что дальше`             | Архитектор  | Следующий шаг из `docs/to-do.md`                       |
| `следующая задача`       | Архитектор  | Первый незавершённый пункт из `docs/to-do.md`          |
```

Стало:

```text
| `обсудим`                | Архитектор  | Обсуждение архитектуры → план в `docs/plan.md`         |
| `что дальше`             | Архитектор  | Первый незавершённый пункт из `docs/to-do.md`          |
```

### Раздел «Типичный рабочий день»

Было:

```text
1. Выбор задачи
   → "следующая задача"

2. Обсуждение
   → "обсудим задачу"
```

Стало:

```text
1. Выбор задачи
   → "что дальше"

2. Обсуждение
   → "обсудим"
```

---

## 3. README.md — убрать инструкции по CC, добавить стек

Удалить раздел `## Разработка` целиком (установка CC, список команд, структура docs/).

Добавить раздел `## Стек` с таблицей технологий проекта (адаптировать под свой стек).
